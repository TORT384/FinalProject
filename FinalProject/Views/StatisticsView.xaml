<UserControl x:Class="FinalProject.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <StackPanel MaxWidth="420"
                    VerticalAlignment="Center">
            <TextBlock FontSize="24"
                       FontWeight="SemiBold"
                       Text="{Binding Title}" />

            <Grid Margin="0,18,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Зіграно ігор:" />
                <TextBlock Grid.Row="0" Grid.Column="1" FontWeight="SemiBold" Text="{Binding TotalGames}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,8,0,0" Text="Перемог:" />
                <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,8,0,0" FontWeight="SemiBold" Text="{Binding Wins}" />

                <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,8,0,0" Text="Відсоток перемог:" />
                <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,8,0,0" FontWeight="SemiBold" Text="{Binding WinRate}" />

                <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,8,0,0" Text="Найкращий час / Середній:" />
                <TextBlock Grid.Row="3"
                           Grid.Column="1"
                           Margin="0,8,0,0"
                           FontWeight="SemiBold"
                           Text="{Binding BestTime}" />
            </Grid>

            <TextBlock Margin="0,8,0,0"
                       FontWeight="SemiBold"
                       Text="{Binding AverageTime}" />

            <Button Width="130"
                    Height="32"
                    Margin="0,18,0,0"
                    HorizontalAlignment="Left"
                    Command="{Binding RefreshCommand}"
                    Content="Оновити" />

            <TextBlock Margin="0,12,0,0"
                       Foreground="#556070"
                       Text="{Binding StatusMessage}" />
        </StackPanel>
    </Grid>
</UserControl>
